# UI分類辞書 Ver0.1（正本・意味／役割）

- **日付**: 2026-09-13（監査承認・軽微修正反映／骨格確定）
- **性質**: **意味・役割の辞書（正本）**。視覚デザイン（px・色・余白・角丸・マーカー色）は含めない → Ver0.2 以降。
- **監査**: **承認（軽微修正付き）**。骨格確定。Hierarchy 意味は Ver0.2 草案へ。
- **適用**:
  - **A. メーカーUI** … 本辞書の対象
  - **B. 生成ホームページ** … **対象外**
- **適用方針**: 段階適用（触った画面だけ合わせる）。意味・命名確定までクラス置換しない。
- **関連**:
  - Hierarchy 意味（草案）: `docs/進行中/2026-09-13-UI分類辞書-Ver0.2-Hierarchy意味設計.md`
  - 実装メモ: `docs/進行中/2026-09-13-UI分類-候補対応表.md`
  - 所在メモ: `docs/進行中/2026-09-13-UI分類-全画面棚卸し.md`
  - 共有: `docs/share/2026-09-13-ui-classify-audit/`

---

## 文書の役割分担

```text
UI分類辞書 Ver0.1（本ファイル・骨格正本）
  ↓ 何者か／第一階層
Ver0.2 Hierarchy意味設計（草案）
  ↓ Content の H1〜H4
候補対応表（実装メモ）
  ↓ 現クラスをどこへ当てるか
全画面棚卸し（所在メモ）
  ↓ どの画面に何があるか
```

将来、画面が増えたら **Content／Interaction／System** を分冊してもよい（Ver0.1では一冊）。

---

## 第一階層

| 親 | 英語メモ | 役割 |
|---|---|---|
| 読む | Content | 読むための情報。Hierarchy（H1〜H4）を持つ |
| 操作する | Interaction | 操作できることを示す。Button／Selection Card／Toggle 等の親 |
| 状態を知らせる | System | **画面内で現在の操作・状態・制約を知らせる情報**。進行・結果・案内を含む |

---

## Content

- Hierarchy は **H1／H2／H3／H4** の4段階のみ（Ver0.1で段数確定）。
- **各段の意味・判定**は `docs/進行中/2026-09-13-UI分類辞書-Ver0.2-Hierarchy意味設計.md`（草案）を参照。
- コンテンツ連動アイコン（⚠ 等）は文字列と一体で、同じ Hierarchy に追従する。

---

## Interaction

親カテゴリ。下位:

| 下位 | 役割 |
|---|---|
| Button | 押して前進・戻る・副操作する |
| Selection Card | **画面・工程・目的など、大きな選択肢を選ぶカード**。Button ではない（見た目がカードでも、小さな同一画面内の択一は Toggle） |
| Toggle | 載せる／載せない、ラジオ的な択一、開示トグル等。**見た目ではなく、「状態を切り替える操作」を表す** |
| Icon Button | ±、▼ など操作示唆の共通部品 |
| **未分類** | Button／Toggle／Selection Card のどれにも無理に押し込まないときの仮置き。後で再分類する |

### Button の3段（日本語寄り）

| 段 | 役割 |
|---|---|
| 本線 | この画面の主前進（確定、これにします 等） |
| 補助 | 戻る、キャンセル、最初に戻す、その他の副操作 |
| 弱操作 | リンク風・弱い操作（ヘルプ、横断リンク等） |

（設計者用英名 Primary／Secondary／Tertiary と対応可）

- **最初に戻す**は補助（破壊寄り・稀用）。Destructive 専用段は Ver0.1 では作らない。

---

## System

**画面内で現在の操作・状態・制約を知らせる情報。**

例: 進行中、成功、注意／エラー、手順の短い案内、文字数カウンタ、コーチ。

- 判定の目安: **「いま何をすればよいか／いまどうなっているか／いまの制約は何か」**
- **コーチ**は System（現在状態・次の一手の説明）。会話・表示名は「コーチ」。内部クラス（`.wizard-coach`）は当面維持。将来 `system-coach` 等へのリネーム可。

---

## 迷ったときの5本

1. **いま何をすればよいか／いまどうなっているか／制約** → System  
2. **何について書いてあるか（項目・見出し・読む本体）** → Content  
3. **画面・工程・目的など大きな選択肢を選ぶカード** → Selection Card  
4. **同じ文脈での状態切替・択一**（見た目不問） → Toggle  
5. **押して前進・戻る・副操作** → Button（戻る／キャンセル／最初に戻すは補助）

どれにも無理なら Interaction／**未分類**（逃げ道）。押し込めない。

---

## 強調表現（将来）

Ver0.1では色・実装を定義しない。

ルールのみ:

- 強調は **稀用**
- **常用しない**
- 1画面あたり原則 0〜1（運用指針）

詳細は Ver0.2 以降。

---

## 変更しないこと（Ver0.1）

- px／rem、色、余白、角丸の確定
- 既存クラス名の一括置換
- 生成ホームページ本文への適用
